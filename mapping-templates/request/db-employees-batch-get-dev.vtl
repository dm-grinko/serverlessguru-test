#set($ids = [])
#foreach($id in ${ctx.args.ids})
    #set($map = {})
    $util.qr($map.put("id", $util.dynamodb.toString($id)))
    $util.qr($ids.add($map))
#end

{
    "version": "2017-02-28"
    "operation": "BatchGetItem",
    "tables": {
        "serverlessguru-dev-employees": {
            "keys": $util.toJson($ids),
            "consistentRead": true
        }
    },
}